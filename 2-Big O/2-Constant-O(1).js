const array = [0,1,2,3,4,5,]
function Constant(arr){
    console.log(arr[2])
}
Constant(array)
 
// Always takes the same time, O(1)
// If an operation takes the same amount of time to execute, no matter how large the input is, it is O(1).
// Also we are not looping thats why constant